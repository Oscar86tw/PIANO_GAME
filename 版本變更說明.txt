PIANO LEARNING V5.3
主題：MASTER CLOCK SYNC + STAFF GEOMETRY FIX

核心修正：
1. 五線譜、示範琴聲、節拍器、節拍判定共用 AudioContext Master Clock
2. BPM 改變時四者同步變速
3. BPM 變化保留當前 beat，不跳拍
4. Metronome 不再靠 setInterval 決定真正拍點
5. Demo Piano 使用 AudioContext timestamp 排程
6. Scoring 改用 Master Beat 判斷 timing

五線譜修正：
- 高音 5 條
- 低音 5 條
- 線距統一 12px
- 音符步距 6px
- Practice 與完整樂譜 Viewer 共用 StaffGeometry
- 高低音譜表列高一致
- Grand Staff 中央間距固定

新增：
- js/core/staff-geometry.js
- js/modules/transport/index.js
- js/modules/demo-scheduler/index.js

檢查：
- 所有 JS 通過 node --check
- Practice 音樂時間不再使用 performance.now
- Scoring 音樂時間不再使用 performance.now
- Metronome 不再使用 setInterval
